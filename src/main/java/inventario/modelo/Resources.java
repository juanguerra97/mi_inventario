package inventario.modelo;

import java.util.Locale;
import java.util.ResourceBundle;

public class Resources {
	
	/**
	 * ResourceBundle con los strings utilizados en las clases del modelo de datos
	 */
	public static ResourceBundle STRINGS_MODELO = ResourceBundle.getBundle("strings/strings_modelo", Locale.getDefault());

	private Resources() {}
	
}
